/**
 * Pruvious client middleware responsible for resolving pages and handling redirects.
 *
 * - @todo
 */
export default defineNuxtRouteMiddleware(() => {
  // @todo
  // - `pruvious-page` (separate logic in `page/utils.client.ts`)
  //   - resolves page
  //   - `usePruviousPage` composable
  //   - handles redirects
  // - also run in `pruvious` middleware
})
