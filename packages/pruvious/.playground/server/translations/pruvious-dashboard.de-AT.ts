import { defineTranslation } from '#pruvious/server'

// Demo regional overrides; everything else inherits from `pruvious-dashboard.de.ts`.
export default defineTranslation({
  Price: 'Preis (AT)',
  Small: 'Klein (AT)',
})
